describe('', () => {
  beforeEach(() => {
    // do something before run tests for each it.
  });

  afterEach(() => {
    // do something after run tests for each it
  });

  it('should be something', () => {
    // arrange

    // act

    // assert

  });

  it('should be something', () => {
    // arrange
    // act
    // assert
  });
});
